(Appended to the built-in prompt.)

Reminders:
- Always include exactly one kind "primer" Schema Primer candidate in addition to any general cards.
- General cards (0–5) go into the shared hive ungated; put durable schema/join/tooling into the primer proposal.
