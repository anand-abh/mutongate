# Card gate

**Disabled on the reflect commit path** for this experiment branch.

- General cards: always lexical upsert (`writeProposedCards`)
- No Schema Primer judge
- See [INITIAL.md](INITIAL.md) for the pinned first-3-steps chat-log card

`gateAndWrite` / `gatePrimerProposal` remain in the codebase for unit tests only.
